apiVersion: v1
kind: Service
metadata:
  name: {{ include "department-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "department-service.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
